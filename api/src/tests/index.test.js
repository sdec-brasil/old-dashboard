/* eslint-disable func-names */
/* eslint-disable no-undef */
import invoice from './invoices/index.test';

describe('Testing API Endpoints', function () {
  describe('Invoice Endpoints', invoice.bind(this));
});
