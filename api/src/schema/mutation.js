// Imports
import { GraphQLObjectType } from 'graphql';

// Mutation
const mutation = new GraphQLObjectType({
  name: 'mutations',
  description: '...',

  fields: {

  },
});

export default mutation;
